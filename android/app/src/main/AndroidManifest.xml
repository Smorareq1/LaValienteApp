<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- La plantilla de Flutter solo declara este permiso en los manifest de
         debug y profile, porque allá lo necesita su propia herramienta. La app
         lo necesita siempre: sin él, un APK de release se instala y arranca,
         pero cada llamada al backend falla y la pantalla de login no pasa de
         "sin conexión". -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="La Valiente"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- "Compartir" desde WhatsApp (y desde la galería, y desde
                 cualquier otra app) entra por aquí. Es la vía real por la que
                 llegan las boletas: alguien fotografía el talonario, lo manda
                 al grupo, y quien captura tendría que guardar la imagen, abrir
                 la app y buscarla en la galería. Con esto la app aparece en la
                 hoja de compartir y se salta los tres pasos.

                 Dos filtros y no uno: `SEND` es una foto y `SEND_MULTIPLE` son
                 varias, que es el caso normal —se comparten las catorce boletas
                 del día de una vez— y sin el segundo Android sencillamente no
                 ofrece la app cuando hay más de una seleccionada.

                 Solo `image/*`: lo que esta app sabe leer es una fotografía. Un
                 PDF o un vídeo compartido aquí solo podría terminar en un error,
                 y aparecer en la lista de una app que no puede hacer el trabajo
                 es peor que no aparecer. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="image/*"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="image/*"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
